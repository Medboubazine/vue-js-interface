export default function example({ from, to, next, store }) {
    //
    console.log("Example Middleware RUN")
    console.log({ from, to, next, store })
    // if condition
    // if (true) {
    // redirect to home
    // next({name: "home"})
    //}
}
